apply plugin: 'com.android.application'

android {
    defaultConfig {
        applicationId "com.profazia.cleanboard"
        compileSdk 37
        minSdkVersion 24
        targetSdkVersion 37
        versionCode 148
        versionName "6.7"
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    namespace 'com.profazia.cleanboard'

    dependenciesInfo {
        includeInApk = false
        includeInBundle = false
    }
}

dependencies {
}
